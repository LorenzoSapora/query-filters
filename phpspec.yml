suites:
    main:
        spec_path: tests
        namespace: LorenzoSapora\QueryFilters
        psr4_prefix: LorenzoSapora\QueryFilters
extensions:
    - PhpSpec\Extension\CodeCoverageExtension
code_coverage:
    format:
      - clover
    output:
      clover: coverage.clover
